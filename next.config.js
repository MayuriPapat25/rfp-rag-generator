/** @type {import('next').NextConfig} */
const nextConfig = {
  // transpilePackages: ['next/font'], // You can remove this line
  // ... other configurations
};

module.exports = nextConfig; // Change to CommonJS export
